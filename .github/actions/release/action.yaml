name: release
description: release go-webvtt
inputs:
  tag:
    description: tag name to be released
    default: ''
  token:
    description: GitHub token
    required: true
runs:
  using: composite
  steps:
  - name: release
    run: |
      echo done
    shell: bash
